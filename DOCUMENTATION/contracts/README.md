# Frozen contracts

**Committed on purpose.** Everything in this directory is a **frozen contract or a generated artifact that
tooling, tests or CI are held to**. It must survive a fresh clone, because the JupyterHub, the test suite
and anyone reproducing a gate all need to see the contract they are being judged against.

This is the deliberate other half of `DESIGN_DOCS/`, which is **gitignored** (`.gitignore:183`) and holds
working design documents — visions, milestone plans, review logs, codex transcripts. Those are arguments in
progress and stay local. A document moves **here** when it stops being an argument and becomes a rule.

| File | What it governs | Status |
|---|---|---|
| **`T0_DECISION_RECORD.md`** | **START HERE.** The single T0 decision record: what T0 froze, the item-by-item completeness check against the T0 exit, the evidence, what is open elsewhere and why none of it blocks, and the **one signature** | **DRAFT v1 — UNSIGNED.** §6 blocks every T1 source edit |
| `T0_0_canonical_contract.md` (v3) | The canonical default-preservation gate for the transport-notebook T1 milestone: the exact invocation, the payload, its normalisation, the two-process harness, and the Hub feasibility thresholds | **DRAFT v3 — unsigned.** §7 blocks every T1 source edit until a named, dated approval; §5.1 qualification has PASSED (evidence in `evidence/t0_qualification/`) |
| `T0_3_claim_support_state.md` (v2) | The `claim_support_state` vocabulary — three states, thirteen reason codes, the ordered gate pipeline and compute truth table. Implemented once, in T1; inherited by the case-study Defensibility criterion | **DRAFT v2** — signs with the T0 record, after T0.2 supplies its tolerances |
| `T0_2b_metrics_and_causal_rule.md` (v3) | The numbers T0.3's vocabulary is evaluated against: metric algorithms (with the interpolation rules that remove output-lattice quantisation), the refinement sequences and stopping rules, the three tolerances, the causal-support rule and its `causal-physics` / `causal-numerical` split, and both matrices | **DRAFT v3** — §2.7 tolerances ACCEPTED 2026-08-20; interpolated metrics are `exp/vN`-only until the JAG |
| `T0_1_C1_v2.md` (v1) | C1 v2 — the enumerated surfaces (generated, not hand-listed), the invariant regression gates and their per-milestone scoping, the versioned change allow-list, and the numeric-rebaseline table. Records two defects found while enumerating: the cache fingerprint misses `disv_grid_utils`, and `transport_base_model` is a change surface but not a dependency surface | **DRAFT v1** |
| `T0_1_pinned_surfaces.md` | Generated list of the 32 result-derived pins in tests and modules — an input to C1's rebaseline map. Not a claim inventory | Generated — do not hand-edit |
| `T0_5_feasibility_probe.md` (v1) | Names the Hub feasibility probe (**G0 / b010210**, the largest real identity), its cold/warm protocol and gating statistic, and the T2-vs-M3/M6 division of labour. Records a predeclared feasibility risk: the mandatory per-group fine run may already be unaffordable | **DRAFT v1** |
| `T0_2a_claim_inventory.json` / `.md` | Generated claim inventory for the six transport notebooks, `tasks_data.py` and the transport modules. Regenerated by `_SUPPORT/src/scripts/transport_claim_inventory.py`, which **fails closed** on any unclassified candidate or orphan classification | Generated — do not hand-edit |

## Rules

1. **A signed contract changes only through its own amendment path** — for the T0 documents that means a
   failure edge back to T0, never an in-flight edit.
2. **Generated artifacts are never hand-edited.** Re-run the generator.
3. **If it is still being argued about, it belongs in `DESIGN_DOCS/`, not here.**
